<snippet>
  <tabTrigger>hook_theme</tabTrigger>
  <content><![CDATA[

/**
 * Implements hook_theme().
 */
function ${TM_FILENAME/([^.]*)\..*$/$1/}_theme(\$existing, \$type, \$theme, \$path) {
  return array(
    '${1}' => array(
      'render element' => '${2}',
      'variables' => array('${3}' => NULL, ${4}),
      'file' => '${5}',
    ),
  );
}

]]></content>
</snippet>
